# Getting Started with With React Hook Forms

Clone the repository and open terminal on the repo directory in your local machine.

## Available Scripts

In the project directory, you can run:

### `npm install`

### `npm start`

Runs the app in the development mode.\
Open [http://localhost:3000](http://localhost:3000) to view it in the browser.
